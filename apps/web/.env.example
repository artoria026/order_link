# Base de datos PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/orderlink"

# Auth.js — genera uno con: openssl rand -base64 32
AUTH_SECRET="cambia-esto-por-un-secreto-seguro-minimo-32-caracteres"

# URL base de la app (sin trailing slash)
NEXTAUTH_URL="http://localhost:3000"
